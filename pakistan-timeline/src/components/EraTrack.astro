---
import EventBubble from './EventBubble.astro';
import EraLabel from './EraLabel.astro';

const { era, events } = Astro.props;
// Each event slot is 300px; add padding for era gutter.
const trackWidth = Math.max(900, events.length * 300 + 200);
---

<section
  class="era-track"
  data-era={era.id}
  style={`--era-color: ${era.color}; min-width: ${trackWidth}px;`}
>
  <EraLabel era={era} />
  <div class="era-rail">
    {events.map((event, i) => (
      <EventBubble event={event} index={i} />
    ))}
  </div>
</section>
